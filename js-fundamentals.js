//Write a JavaScript function that accepts an array as parameter and  returns the elements of an array of numbers that are less than 18.

const number = [ 30, 15, 12, 21, 19];

const filtered= number.filter(n => n < 18 );

console.log(filtered);